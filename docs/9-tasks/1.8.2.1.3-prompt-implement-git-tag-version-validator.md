# Task 1.8.2.1.3: Implement Git Tag Version Validator

## Scenario

Given a tagged release
When I validate version consistency
Then manifest.json version should match the Git tag

## Instructions for Coding LLM

Implement a Git tag version validator:

- Develop a function to fetch the latest Git tag
- Create a parser to extract the version number from the tag
- Implement validation for proper semantic versioning format in tags

Please implement the Git tag version validator based on the above instructions. This function will be crucial for ensuring that the version specified in Git tags follows the correct format and can be accurately compared with versions from other sources like manifest.json.
