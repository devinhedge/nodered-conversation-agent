# Task 1.2.2.2: Create Unit Tests for File Organization Compliance Check

## Scenario

Given the integration directory structure
When I check file organization
Then Python modules should be in the integration root directory
And configuration schemas should be properly organized
And no integration files should exist outside the designated directory

## Instructions for Coding LLM

1. Create unit tests to cover all aspects of the acceptance criteria:
   - Test with correctly organized files
   - Test with misplaced files (e.g., Python modules in subdirectories)
   - Test with files outside the designated directory

2. Implement tests for error handling and logging:
   - Verify that appropriate error messages are generated for different organization issues
   - Check that detailed information is logged during the compliance check process

3. Create tests for different types of HACS integrations:
   - Test with various integration types and their specific file organization requirements
   - Verify that the function correctly handles different integration structures

4. Implement performance tests:
   - Create tests with large integrations containing many files
   - Measure and assert on the execution time of the compliance check function

5. Test integration with the larger HACS validation suite:
   - Mock the environment of the HACS validation suite
   - Verify that the compliance check function can be called and returns results as expected within the suite

6. Create tests for common file organization mistakes and anti-patterns:
   - Implement tests that check for specific HACS-related file organization issues
   - Verify that these issues are correctly identified and reported

7. Implement edge case tests:
   - Test with empty directories
   - Test with non-standard file names or extensions
   - Test with symlinks or hidden files

Please implement comprehensive unit tests for the file organization compliance check function based on the above instructions. Ensure that all possible scenarios are covered and that the tests are easily maintainable.
