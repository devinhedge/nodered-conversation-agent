# Task 1.8.1.1.2: Implement Version Consistency Checks

## Scenario

Given a HACS integration repository
When I create a new release
Then the release tag should match the manifest.json version
And release notes should document changes and fixes
And release should be properly tagged in Git
And HACS should detect the new version within 24 hours

## Instructions for Coding LLM

Implement version consistency checks:

- Create a function to compare the manifest.json version with the Git tag
- Implement checks to ensure the release tag matches the manifest version
- Add validation to prevent releases with mismatched versions

Please implement version consistency checks based on the above instructions. Ensure that the implementation includes functions to compare versions across different sources (manifest.json and Git tags), implements checks for version matching, and adds validation to prevent releases with inconsistent versions. This feature is crucial for maintaining the integrity and reliability of the release process.
