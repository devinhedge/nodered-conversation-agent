# Task 1.3.1.2: Create Unit Tests for HACS Manifest Fields Validation

## Scenario

Given a manifest.json file in the integration directory
When I validate the manifest content
Then the file should include a "version" field with semantic versioning
And the file should include "documentation" field with valid URL
And the file should include "issue_tracker" field with GitHub issues URL
And the file should include "codeowners" field with GitHub usernames

## Instructions for Coding LLM

1. Create unit tests to cover all aspects of the acceptance criteria:
   - Test with valid manifest files containing all required fields
   - Test with manifest files missing one or more required fields
   - Test with invalid field values (e.g., incorrect version format, invalid URLs)

2. Implement tests for error handling and logging:
   - Verify that clear, actionable error messages are provided for missing or invalid fields
   - Ensure that detailed information about the validation process is logged

3. Create tests for different versions of HACS manifest requirements:
   - Test the function's ability to handle potential future changes in manifest requirements

4. Implement tests for the GitHub username validation helper function:
   - Test with valid and invalid GitHub usernames
   - Mock API responses for GitHub username validation

5. Create tests for the correction suggestion feature:
   - Verify that appropriate suggestions are provided for invalid manifest entries

6. Implement performance tests:
   - Measure and assert on the execution time of the validation function
   - Test the optimization for external resource validation (e.g., URL validation)

7. Create integration tests:
   - Test the validation function as part of a simulated pre-commit hook or CI/CD pipeline

8. Implement edge case tests:
   - Test with empty manifest files
   - Test with malformed JSON
   - Test with extremely large manifest files

Please implement the unit tests for the required HACS manifest fields validation function based on the above instructions. Ensure comprehensive coverage of all acceptance criteria, error handling, different HACS versions, performance optimizations, and edge cases.
