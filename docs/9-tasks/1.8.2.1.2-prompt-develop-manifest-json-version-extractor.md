# Task 1.8.2.1.2: Develop Manifest.json Version Extractor

## Scenario

Given a tagged release
When I validate version consistency
Then manifest.json version should match the Git tag

## Instructions for Coding LLM

Develop a manifest.json version extractor:

- Create a function to parse manifest.json and extract the version field
- Implement error handling for malformed JSON or missing version information
- Consider caching the extracted version for performance in repeated checks

Please implement the manifest.json version extractor based on the above instructions. This function will be an essential component in ensuring version consistency across the project, particularly for validating the version specified in the manifest.json file against other version references.
