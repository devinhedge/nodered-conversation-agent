# Task 1.1.1.1.2: Verify Single Integration Subdirectory

## Scenario

Given a custom component exists in a GitHub repository
When I verify the repository structure
Then there should be only one subdirectory under `/custom_components/`

## Instructions for Coding LLM

Implement a function to verify that there's only one subdirectory under `/custom_components/`:

- List all subdirectories within the `/custom_components/` directory
- Check that the number of subdirectories is exactly one
- Return a boolean value indicating whether this condition is met
- If more than one subdirectory exists, log the names of all found subdirectories
- Implement error handling for cases where the directory cannot be accessed or read

Please implement this function to ensure that only one integration exists in the repository, as part of the repository structure validation process.
