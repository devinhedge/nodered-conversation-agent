# Task 1.1.2.2.4: Implement Mock GitHub API Responses

## Scenario

Given a need to test GitHub repository access validation
When I create unit tests
Then I should have mock GitHub API responses to simulate various scenarios

## Instructions for Coding LLM

Implement mock GitHub API responses for testing repository access validation:

1. Create a mock response for a public repository with correct settings:
   - Include mock data for repository visibility (public)
   - Include mock data for cloning permissions (allowed)
   - Include mock data for an open source license (e.g., MIT license)

2. Create a mock response for a private repository:
   - Include mock data for repository visibility (private)
   - Include mock data for restricted cloning permissions

3. Create a mock response for a repository with missing open source license:
   - Include mock data for repository visibility (public)
   - Include mock data for cloning permissions (allowed)
   - Omit or provide null data for the license field

4. Create a mock response for a repository with ambiguous settings:
   - Include mock data with unclear visibility settings
   - Include mock data with ambiguous cloning permissions

5. Implement mock responses for API rate limiting scenarios:
   - Create a mock response simulating an API rate limit error
   - Include appropriate headers and error messages

6. Create mock responses for different types of API errors:
   - Implement a mock for a 404 Not Found error
   - Implement a mock for a 500 Internal Server Error
   - Implement a mock for a network timeout scenario

7. Ensure the mock responses are easily configurable:
   - Create a system to easily switch between different mock responses in tests
   - Allow for easy modification of specific fields in the mock responses

Please implement these mock GitHub API responses based on the above instructions. Ensure that the mocks are comprehensive, covering various scenarios needed for thorough testing of the repository access validation function.
